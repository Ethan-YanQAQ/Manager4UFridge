# 智能冰箱食材管理系统 — 模块间工作流程框图

```mermaid
flowchart TB
    subgraph HARDWARE["🔧 硬件终端 (STM32H743)"]
        direction TB
        CAM[📷 OV5640 摄像头<br/>DCMI + DMA 双缓冲]
        DOOR[🚪 霍尔传感器<br/>开门/关门检测]
        GAS[🫧 MQ135/MQ137<br/>气体传感器 ADC]
        QSPI[💾 QSPI Flash<br/>W25Q256 32MB<br/>YOLO 权重存储]
        SDRAM[🧠 SDRAM<br/>W9825G6KH 32MB<br/>帧缓冲 + 中间张量]

        subgraph AI_SOC["⚡ AI 推理引擎"]
            PREPROC[图像预处理<br/>缩放/归一化]
            INFER[YOLO11n INT8<br/>Cube.AI Runtime]
            POSTPROC[后处理<br/>NMS + 类别解析]
        end

        WiFi[📡 ESP-01S WiFi<br/>AT 指令 / HTTP POST]

        DOOR -->|"开门中断"| CAM
        CAM -->|"JPEG 帧"| SDRAM
        SDRAM -->|"640×480 → 320×320"| PREPROC
        QSPI -->|"权重 2.48MB"| INFER
        PREPROC --> INFER
        INFER --> POSTPROC
        POSTPROC -->|"检测结果 JSON"| WiFi

        GAS -->|"气体浓度"| WiFi
        DOOR -->|"开关门事件"| WiFi
    end

    subgraph CLOUD["☁️ 微信云开发 (CloudBase)"]
        direction TB
        CFUNC[云函数<br/>addDetection<br/>getInventory<br/>getHistory<br/>aiAdvisor]

        DB[(🗄️ 云数据库<br/>inventory 表<br/>history 表)]

        CFUNC --> DB
        DB --> CFUNC
    end

    subgraph MINIAPP["📱 微信小程序 (uni-app + Vue3)"]
        direction TB
        HOME[🏠 首页<br/>库存概览 + AI 助手]
        INV[📦 库存页<br/>新鲜度色标<br/>FRESH/WARNING/EXPIRED]
        HIST[📋 记录页<br/>存取时间线]
        ALERT[⚠️ 预警页<br/>过期/临期提醒]
        CHAT[💬 AI 对话<br/>菜谱推荐<br/>采购建议]
    end

    WiFi -->|"HTTP POST<br/>检测结果"| CFUNC
    CFUNC -->|"查询/更新"| DB
    DB -->|"wx.cloud.database()"| HOME
    HOME --> INV
    HOME --> ALERT
    HOME --> CHAT
    INV --> HIST

    style HARDWARE fill:#E3F2FD,stroke:#1565C0
    style CLOUD fill:#E8F5E9,stroke:#2E7D32
    style MINIAPP fill:#FFF3E0,stroke:#E65100
    style AI_SOC fill:#F3E5F5,stroke:#7B1FA2
```

## 工作流程说明

### 🚪 门开关 → 拍照 → AI 推理 → 上传

```
开门 → GPIO 中断 → 霍尔传感器检测
      ↓
  DCMI 启动 → OV5640 拍"开门前"照片 → SDRAM 帧缓冲
      ↓
  关门 → 再次拍照 → 两帧送入 AI 推理引擎
      ↓
  YOLO11n INT8 (QSPI 权重) → 检测食材类别 + 置信度 + 边界框
      ↓
  IoU 贪心匹配（前后帧对比）→ STILL / ADDED / REMOVED
      ↓
  ESP-01S HTTP POST → 云函数 addDetection → 写入云数据库
      ↓
  小程序实时刷新 → 库存更新 + 新鲜度重新计算
```

### 🫧 气体传感器 → 腐坏检测

```
MQ135/MQ137 定时采样 → ADC 读取 → 阈值判断
      ↓
  超出阈值 → HTTP POST 上报告警 → 云数据库写入
      ↓
  小程序预警页 → 红色标记 + 推送通知
```

### 🤖 AI 对话助手

```
用户在聊天页输入 → 云函数 aiAdvisor → DeepSeek API
      ↓                            ↓
  小程序展示回复        读取当前库存 + 历史记录作为上下文
  (菜谱/采购/分析)      返回自然语言回复
```
